#include <stdio.h>

int main()
{
    int num1, num2;
    num1 = 20;
    num2 = 6;
    printf("sum of two integer is =%d\n", num1 + num2);
    printf("differance of two integer is=%d\n", num1 - num2);
    printf("product of two integer is=%d\n", num1 * num2);
    printf("quotient and remainder of two integer=%d,%d\n", num1 / num2, num1 % num2);
    return 0;
}